name: Build and Publish to TestFlight

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    timeout-minutes: 60

    steps:

    ############################################################################
    - name: Show Environment Info
      run: |
        sw_vers
        xcode-select -p
        xcodebuild -version
        xcodebuild -showsdks
        df -h

    ############################################################################
    - name: Checkout Code
      uses: actions/checkout@v4

    ############################################################################
    - name: Organize Source Dirs
      run: |
        mkdir -p src/
        rsync -av --exclude="src" --exclude=".git" $GITHUB_WORKSPACE/ src/

    ############################################################################
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    ############################################################################
    - name: Install Build Tools
      run: |
        brew install autoconf automake libtool tree rsync
        pip install --upgrade pip
        pip install Cython==3.0.11
        pip install kivy-ios
        pip install anthropic openai requests

    ############################################################################
    - name: Import Code Signing Certificate
      env:
        APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
        APPLE_PRIVATE: ${{ secrets.APPLE_PRIVATE }}
      run: |
        # Decode and save certificate and private key
        echo "$APPLE_CERTIFICATE" | base64 --decode > ios_distribution.cer
        echo "$APPLE_PRIVATE" | base64 --decode > ios_private_key.pem

        # Create a temporary keychain
        security create-keychain -p temp-password build.keychain

        # Import certificate and private key into the keychain
        security import ios_distribution.cer -k build.keychain -T /usr/bin/codesign
        security import ios_private_key.pem -k build.keychain -T /usr/bin/codesign

        # Add the keychain to the search list and prioritize it
        security list-keychains -s build.keychain
        security default-keychain -s build.keychain

        # Set keychain settings to prevent automatic lock
        security set-keychain-settings -lut 7200 build.keychain

        # Unlock the keychain
        security unlock-keychain -p temp-password build.keychain

        # Allow codesign to access the signing identity without prompts
        security set-key-partition-list -S apple-tool:,apple: -s -k temp-password build.keychain

    ############################################################################
    - name: Kivy iOS Build
      env:
        ACTIONS_STEP_DEBUG: false
      run: |
        toolchain build python3 kivy

    ############################################################################
    - name: Create Xcode Project
      env:
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        WHOP_API_KEY: ${{ secrets.WHOP_API_KEY }}
        WHOP_COMPANY_ID: ${{ secrets.WHOP_COMPANY_ID }}
      run: |
        toolchain create ElectriciansNow src/

        cd ElectriciansNow-ios

        # Update bundle identifier
        sed -i '' 's/org\.kivy\..*now/com.snslocation.electricians-now/g' ElectriciansNow-Info.plist

        # Update deployment target for iOS 17+
        sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = [0-9]*\.[0-9]*/IPHONEOS_DEPLOYMENT_TARGET = 17.0/g' ElectriciansNow.xcodeproj/project.pbxproj

        # Configure code signing
        sed -i '' '/CODE_SIGN_IDENTITY/c\
                    CODE_SIGN_IDENTITY = "iPhone Distribution";\
                    DEVELOPMENT_TEAM = MNGTD992QD;\
                    PRODUCT_BUNDLE_IDENTIFIER = com.snslocation.electricians-now;' \
                    ElectriciansNow.xcodeproj/project.pbxproj

        # Disable bitcode (deprecated in Xcode 14+)
        sed -i '' 's/ENABLE_BITCODE = YES/ENABLE_BITCODE = NO/g' ElectriciansNow.xcodeproj/project.pbxproj

        tree -a -L 2

    ############################################################################
    - name: Update Kivy Project
      run: |
        toolchain update ElectriciansNow-ios

    ############################################################################
    - name: Add App Icons
      run: |
        cp src/Resources/AppIcon.appiconset/* ElectriciansNow-ios/ElectriciansNow/Images.xcassets/AppIcon.appiconset/

    ############################################################################
    - name: Setup Fastlane
      run: |
        gem install fastlane

    ############################################################################
    - name: Fastlane Match
      env:
        APP_STORE_API_KEY: ${{ secrets.APP_STORE_API_KEY }}
        API_KEY_P8: ${{ secrets.API_KEY_P8 }}
        FASTLANE_USER: ${{ secrets.APPLE_USER }}
        FASTLANE_PASSWORD: ${{ secrets.APPLE_PASS }}
        MATCH_PASSWORD: ${{ secrets.APPLE_PASS }}
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        echo "$APP_STORE_API_KEY" | base64 --decode > api_key.json
        echo "$API_KEY_P8" | base64 --decode > api_key.p8
        # Let match regenerate expired certificates automatically
        fastlane run match force_for_new_certificates:true --verbose

    ############################################################################
    - name: Build and Export App
      env:
        FASTLANE_TEAM_ID: MNGTD992QD
        FASTLANE_USER: ${{ secrets.APPLE_USER }}
        FASTLANE_PASSWORD: ${{ secrets.APPLE_PASS }}
      run: |
        # List available schemes
        xcodebuild -list -project ElectriciansNow-ios/ElectriciansNow.xcodeproj

        # Build with fastlane
        fastlane gym --verbose

    ############################################################################
    - name: IPA Artifact Upload
      uses: actions/upload-artifact@v4
      with:
        name: ElectriciansNow-ipa
        path: ElectriciansNow-ios/build/ElectriciansNow.ipa

    ############################################################################
    - name: TestFlight Upload
      env:
        FASTLANE_TEAM_ID: MNGTD992QD
        FASTLANE_USER: ${{ secrets.APPLE_USER }}
        FASTLANE_PASSWORD: ${{ secrets.APPLE_PASS }}
      run: |
        fastlane pilot upload \
          --api_key_path api_key.json \
          --ipa ElectriciansNow-ios/build/ElectriciansNow.ipa \
          --skip_waiting_for_build_processing true

    ############################################################################
    - name: Add External Beta Testers
      env:
        FASTLANE_TEAM_ID: MNGTD992QD
        FASTLANE_USER: ${{ secrets.APPLE_USER }}
        FASTLANE_PASSWORD: ${{ secrets.APPLE_PASS }}
      run: |
        # Add external testers - they'll receive Apple's TestFlight invite email
        fastlane pilot add \
          --api_key_path api_key.json \
          nikola.bulatovic@aol.com \
          --group "External Testers" || true

    ############################################################################
    - name: Notify Tester via Email
      if: success()
      run: |
        curl -s -X POST "https://gcchelp-email-worker.gcchelp.workers.dev/send" \
          -H "Content-Type: application/json" \
          -H "X-Webhook-Secret: V73S-urN7L9RJY4YiLU-x51IiRtCNCsFhO-XSZ_41fo" \
          -d '{
            "to": "nikola.bulatovic@aol.com",
            "subject": "ElectriciansNow Build Ready for Testing",
            "from": "info@gcchelp.com",
            "fromName": "ElectriciansNow CI",
            "html": "<h2>New Build Available!</h2><p>A new ElectriciansNow build has been uploaded to TestFlight.</p><p><strong>How to install:</strong></p><ol><li>Open <a href=\"https://apps.apple.com/us/app/testflight/id899247664\">TestFlight</a> on your iPhone</li><li>You should see ElectriciansNow - tap Update/Install</li><li>If you do not see it, check your email for an Apple TestFlight invite</li></ol><p><strong>What to test:</strong></p><ul><li>Get an AI estimate for electrical work</li><li>Schedule a service and pay via the checkout</li><li>Apple Pay is available on the payment page</li></ul><p>Thanks for testing!</p>",
            "text": "New ElectriciansNow build uploaded to TestFlight. Open TestFlight on your iPhone to install. Test: get an estimate, schedule service, and pay."
          }'
